{
  "name": "being-a-woman-of-faith-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "echo 'Building backend...'",
    "watch": "nodemon --watch src -e js,json --exec 'npm run build && npm start'"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
