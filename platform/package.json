{
  "name": "being-a-woman-of-faith-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/config.js",
    "dev": "nodemon src/config.js",
    "build": "echo 'Building platform...'",
    "watch": "nodemon --watch src -e js,json --exec 'npm run build && npm start'"
  },
  "dependencies": {
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
